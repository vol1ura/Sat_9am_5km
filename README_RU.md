# Sat 9am 5km - Система управления забегами

[![Quality&Tests](https://github.com/vol1ura/Sat_9am_5km/actions/workflows/rubyonrails.yml/badge.svg)](https://github.com/vol1ura/Sat_9am_5km/actions/workflows/rubyonrails.yml)
[![CodeQL](https://github.com/vol1ura/Sat_9am_5km/actions/workflows/codeql.yml/badge.svg)](https://github.com/vol1ura/Sat_9am_5km/actions/workflows/codeql.yml)
[![Code Coverage](https://qlty.sh/gh/vol1ura/projects/Sat_9am_5km/coverage.svg)](https://qlty.sh/gh/vol1ura/projects/Sat_9am_5km)
[![Maintainability](https://qlty.sh/gh/vol1ura/projects/Sat_9am_5km/maintainability.svg)](https://qlty.sh/gh/vol1ura/projects/Sat_9am_5km)

[![GPLv3 license](https://img.shields.io/badge/License-GPLv3-blue.svg)](http://perso.crans.org/besson/LICENSE.html)
![Contributions](https://img.shields.io/badge/Contributions-Welcome-brightgreen)
![Website](https://img.shields.io/website?down_color=red&down_message=failed&up_color=blue&up_message=online&url=https%3A%2F%2Fs95.ru)

## Описание проекта

**Sat 9am 5km (S95)** — это веб-платформа для управления независимыми парковыми забегами, которые проводятся по основным принципам приостановленного в России паркрана: суббота, 9 утра, дистанция 5 км.

Платформа объединяет бегунов, волонтёров и организаторов забегов, предоставляя инструменты для:
- Регистрации и управления забегами
- Отслеживания результатов участников
- Управления волонтёрами
- Награждения участников знаками отличия (бейджами)
- Социальных функций (друзья, клубы, рейтинги)
- Мобильного приложения для волонтёров

## Основные возможности

### Для участников
- 📊 Просмотр результатов забегов
- 🏃 Отслеживание личных рекордов и статистики
- 🏆 Получение бейджей за достижения
- 👥 Социальные функции (друзья, подписки)
- 🏅 Рейтинги и дуэли с другими участниками
- 📱 Мобильное приложение для отслеживания прогресса

### Для организаторов
- 🎯 Управление событиями и активностями
- 📝 Обработка результатов забегов
- 👨‍💼 Управление волонтёрами и позициями
- 📈 Статистика и аналитика
- 🔔 Уведомления участников

### Для волонтёров
- ⏱️ Мобильное приложение с таймером
- 📷 Сканер штрих-кодов для финиша
- 📊 Управление живыми результатами
- 📱 Удобный интерфейс для работы на забеге

## Технологический стек

- **Backend**: Ruby on Rails 8.0
- **Ruby**: 3.4.5
- **База данных**: PostgreSQL 17.2
- **Кэширование**: Redis
- **Очереди задач**: Sidekiq
- **Фронтенд**: Bootstrap 5.3, Hotwire (Turbo/Stimulus)
- **Контейнеризация**: Docker & Docker Compose
- **Развертывание**: Capistrano
- **Мониторинг**: Rollbar, Rails Performance, PgHero

## Структура проекта

```
Sat_9am_5km/
├── app/
│   ├── admin/              # Административная панель (ActiveAdmin)
│   ├── assets/             # Статические ресурсы (CSS, JS, изображения)
│   ├── controllers/        # Контроллеры приложения
│   │   ├── api/            # API контроллеры
│   │   └── ...
│   ├── helpers/            # Хелперы для представлений
│   ├── javascript/         # JavaScript код (Stimulus контроллеры)
│   ├── jobs/               # Фоновые задачи (ActiveJob)
│   ├── mailers/            # Почтовые шаблоны
│   ├── models/             # Модели данных (ActiveRecord)
│   ├── services/           # Бизнес-логика (сервисные объекты)
│   └── views/              # Представления (ERB шаблоны)
├── config/                 # Конфигурационные файлы
│   ├── routes.rb           # Маршруты приложения
│   ├── database.yml        # Настройки базы данных
│   ├── credentials.yml.enc # Зашифрованные секреты
│   └── ...
├── db/                     # Миграции и схема базы данных
├── spec/                   # Тесты (RSpec)
├── lib/                    # Дополнительные библиотеки
├── public/                 # Публичные файлы
└── deploy/                 # Конфигурация развертывания
```

## Установка и настройка

### Требования

- Docker и Docker Compose
- Git
- (Опционально) Ruby 3.4.5 и PostgreSQL для локальной разработки без Docker

### Быстрый старт с Docker

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/vol1ura/Sat_9am_5km.git
cd Sat_9am_5km
```

2. **Создайте необходимые конфигурационные файлы:**
```bash
cp ./deploy/.env.example ./deploy/.env
cp ./config/database.yml.example ./config/database.yml
```

3. **Отредактируйте файлы конфигурации:**
   - `deploy/.env` — переменные окружения
   - `config/database.yml` — настройки базы данных

4. **Соберите и запустите проект:**
```bash
make build
make ash
```

5. **Внутри Docker контейнера выполните:**
```bash
rails db:prepare
rake db:parse_parkrun_codes[kuzminki_db.csv]
rails db:environment:set RAILS_ENV=test
rails credentials:edit --environment test
rspec
```

6. **Запустите приложение:**
```bash
make
```

### Доступные URL в режиме разработки

- **Сайт**: [http://localhost:3000](http://localhost:3000)
- **Админ-панель**: [http://localhost:3000/admin](http://localhost:3000/admin)
- **Превью писем**: [http://localhost:3000/rails/mailers](http://localhost:3000/rails/mailers)
- **Панель базы данных**: [http://localhost:3003](http://localhost:3003/?pgsql=db&username=postgres)
- **Sidekiq WebUI**: [https://s95.ru/sidekiq](https://s95.ru/sidekiq) (только для администраторов)
- **Rails Performance**: [dev](http://localhost:3000/app_performance/) или [prod](https://s95.ru/app_performance/) (только для администраторов)
- **Postgres Performance**: [dev](http://localhost:3000/pg_stats) или [prod](https://s95.ru/pg_stats) (только для администраторов)
- **Active Storage Dashboard**: [dev](http://localhost:3000/storage) или [prod](https://s95.ru/storage) (только для администраторов)

## Разработка

### Полезные команды Makefile

```bash
make          # Запустить проект (docker compose up -d)
make build    # Собрать Docker образ
make ash      # Войти в контейнер (ash shell)
make rc       # Открыть Rails console
make psql     # Открыть PostgreSQL console
make bind     # Подключиться к запущенному контейнеру
make checkup  # Проверить код с помощью RuboCop
```

### Структура базы данных

Основные модели:

- **User** — пользователи системы
- **Athlete** — спортсмены (участники забегов)
- **Event** — события (забеги)
- **Activity** — конкретные активности (забег в определенную дату)
- **Result** — результаты участников
- **Volunteer** — волонтёры
- **Badge** — знаки отличия
- **Trophy** — награды спортсменов
- **Club** — клубы
- **Friendship** — дружеские связи между спортсменами

### Тестирование

Проект использует RSpec для тестирования:

```bash
# Запустить все тесты
rspec

# Запустить тесты конкретного файла
rspec spec/models/user_spec.rb

# Запустить тесты с покрытием
COVERAGE=true rspec
```

### Линтинг

Проект использует RuboCop для проверки стиля кода:

```bash
# Проверить код
rubocop

# Автоматически исправить проблемы
rubocop -a

# Проверить только ошибки
make checkup
```

### Работа с секретами

```bash
# Редактировать секреты для production
rails credentials:edit

# Редактировать секреты для test
rails credentials:edit --environment test

# Редактировать секреты для development
rails credentials:edit --environment development
```

## API Документация

### Внутренний API (`/api/internal`)

Доступен только с localhost (127.0.0.1).

#### Создание пользователя
```
POST /api/internal/user
Content-Type: application/json

{
  "user": {
    "email": "user@example.com",
    "password": "password",
    "first_name": "Иван",
    "last_name": "Иванов",
    "telegram_id": 123456789,
    "telegram_user": "username"
  },
  "athlete": {
    "name": "Иван Иванов",
    "male": true,
    "parkrun_code": 123456
  }
}
```

#### Генерация ссылки авторизации
```
POST /api/internal/user/auth_link
Content-Type: application/json

{
  "user_id": 1,
  "domain": "ru"
}
```

### Мобильный API (`/api/mobile`)

#### Информация об атлете
```
GET /api/mobile/athletes/:code/info?locale=ru
```

#### Отправка результатов с таймера
```
POST /api/mobile/activities/stopwatch
Content-Type: application/json

{
  "token": "activity_token",
  "results": [
    {
      "position": 1,
      "total_time": "00:18:30"
    }
  ]
}
```

#### Отправка результатов со сканера
```
POST /api/mobile/activities/scanner
Content-Type: application/json

{
  "token": "activity_token",
  "results": [
    {
      "position": "P1",
      "code": "A123456"
    }
  ]
}
```

#### Отправка живых результатов
```
POST /api/mobile/activities/live
Content-Type: application/json

{
  "token": "activity_token",
  "activityStartTime": 1234567890,
  "results": [
    {
      "position": 1,
      "total_time": "00:18:30.45"
    }
  ]
}
```

### Parkzhrun API (`/api/parkzhrun`)

Требует авторизации через заголовок `Authorization` с API ключом.

#### Обновление атлета
```
PATCH /api/parkzhrun/athletes/:id
Authorization: <api_key>
Content-Type: application/json

{
  "name": "Иван Иванов",
  "male": true
}
```

#### Создание активности
```
POST /api/parkzhrun/activities
Authorization: <api_key>
Content-Type: application/json

{
  "event_id": 1,
  "date": "2024-01-06",
  "description": "Описание забега"
}
```

## Фоновые задачи (Jobs)

Приложение использует Sidekiq для выполнения фоновых задач:

- **ResultsProcessingJob** — обработка результатов забега
- **AthletesAwardingJob** — награждение спортсменов бейджами
- **TimerProcessingJob** — обработка данных с таймера
- **ScannerProcessingJob** — обработка данных со сканера
- **AthleteStatsUpdateJob** — обновление статистики спортсмена
- **BreakingTimeAwardingJob** — награждение за улучшение времени
- **FivePlusAwardingJob** — награждение за участие 5+ недель подряд
- **HomeBadgeAwardingJob** — награждение домашним бейджем
- **MinuteBingoAwardingJob** — награждение за "минутное бинго"
- **FullProfileAwardingJob** — награждение за заполненный профиль
- **FunrunAwardingJob** — награждение за участие в funrun
- **RenewGoingToEventJob** — обновление списка участников забега

## Планировщик задач (Cron)

Приложение использует `whenever` для планирования задач:

- **Ежедневно в 2:00** — резервное копирование базы данных и хранилища
- **Ежедневно в 23:30** — награждение за полный профиль
- **Каждый четверг в 18:00** — уведомления об истечении бейджей "rage"
- **Каждую пятницу в 1:00** — очистка статистики запросов PgHero
- **Каждую пятницу в 2:00** — уведомления волонтёрам
- **Каждую пятницу в 10:00** — приглашения новичков
- **Каждую пятницу в 11:00** — обновление списков участников забегов
- **Каждую субботу в 18:00** — уведомления об истечении бейджей "breaking time"
- **Каждую субботу в 23:00** — еженедельное резервное копирование
- **Каждое воскресенье в 5:00** — синхронизация с Parkzhrun
- **Каждое воскресенье в 5:05** — обработка результатов
- **Каждое воскресенье в 5:10** — награждение бейджами
- **Каждое воскресенье в 5:15** — награждение домашними бейджами
- **Каждое воскресенье в 5:30** — генерация sitemap
- **Каждое воскресенье в 5:35** — награждение "минутным бинго"
- **Каждое воскресенье в 5:45** — обновление статистики спортсменов
- **Каждое воскресенье в 12:00** — уведомления о дублированных результатах
- **Каждое воскресенье в 12:05** — уведомления о некорректных активностях
- **Каждое воскресенье в 12:10** — уведомления о некорректных бегущих волонтёрах
- **Ежемесячно в 1:00** — очистка неприкреплённых файлов
- **Ежемесячно в 6:00** — установка дат трофеев "five plus"
- **Каждые 4 месяца в 1:00** — очистка кэша Bootsnap

## Развертывание

### Требования для сервера

- Ubuntu Server (рекомендуется)
- Ruby 3.4.5 (через rbenv)
- PostgreSQL 17.2
- Redis
- Nginx (для reverse proxy)
- Systemd (для управления процессами)

### Установка зависимостей на сервере

1. **Установите системные зависимости:**
```bash
sudo apt-get install -y libyaml-dev
```

2. **Установите Rust (для сборки Ruby с YJIT):**
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

3. **Установите rbenv:**
```bash
curl -fsSL https://github.com/rbenv/rbenv-installer/raw/HEAD/bin/rbenv-installer | bash
```

4. **Установите Ruby:**
```bash
rbenv install 3.4.5
rbenv global 3.4.5
```

### Развертывание с Capistrano

Проект использует Capistrano для автоматического развертывания:

```bash
# Развернуть на production
cap production deploy

# Откатить последнее развертывание
cap production deploy:rollback
```

### Конфигурация сервисов

Примеры конфигурационных файлов находятся в `deploy/`:
- `puma.service.example` — systemd сервис для Puma
- `puma.socket.example` — systemd socket для Puma
- `sidekiq.service.example` — systemd сервис для Sidekiq
- `logrotate.example` — конфигурация ротации логов

## Мониторинг и обслуживание

### Сброс статистики PostgreSQL

```sql
SELECT pg_stat_statements_reset();
```

### Мониторинг производительности

- **Rollbar**: [https://app.rollbar.com/a/Urka/fix/items](https://app.rollbar.com/a/Urka/fix/items) — отслеживание ошибок
- **UptimeRobot**: [https://dashboard.uptimerobot.com/monitors/797544445](https://dashboard.uptimerobot.com/monitors/797544445) — мониторинг доступности
- **Rails Performance**: `/app_performance` — производительность приложения
- **PgHero**: `/pg_stats` — статистика PostgreSQL
- **Sidekiq WebUI**: `/sidekiq` — управление очередями задач

## Безопасность

- Аутентификация через Devise
- Авторизация через CanCanCan
- Защита от CSRF атак
- Rate limiting через Rack::Attack
- Аудит изменений через Audited
- Шифрование секретов через Rails Credentials

## Локализация

Приложение поддерживает три языка:
- Русский (ru) — основной
- Сербский (rs)
- Английский (en)

Файлы локализации находятся в `config/locales/`.

## Лицензия

Проект распространяется под лицензией GPLv3. См. файл [LICENSE](LICENSE) для подробностей.

## Вклад в проект

Мы приветствуем вклад в развитие проекта! Пожалуйста, ознакомьтесь с [CONTRIBUTING.md](CONTRIBUTING.md) и [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md) перед началом работы.

### Процесс внесения изменений

1. Форкните репозиторий
2. Создайте ветку для новой функции: `git checkout -b my-new-feature`
3. Внесите изменения и добавьте тесты
4. Убедитесь, что все тесты проходят: `rspec`
5. Проверьте код линтером: `rubocop`
6. Закоммитьте изменения: `git commit -am 'Add some feature'`
7. Отправьте изменения: `git push origin my-new-feature`
8. Создайте Pull Request

## Контакты

- **Веб-сайт**: [https://s95.ru](https://s95.ru)
- **Email**: vol1ura@gmail.com

## Благодарности

Проект создан для сообщества бегунов, которые ценят регулярные субботние забеги и дух паркрана.

