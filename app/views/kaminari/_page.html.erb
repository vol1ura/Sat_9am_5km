<% if page.current? %>
  <span class="page current" aria-current="page"><%= page %></span>
<% else %>
  <span class="page">
    <%= link_to page, url, rel: page.rel, remote: remote %>
  </span>
<% end %>
