<%= render Ui::CardComponent.new(class: 'h-full border-brand/25') do |card| %>
  <% card.with_header do %>
    <div class="border-b border-line px-4 py-3">
      <h3 class="mb-0 text-lg font-semibold text-ink"><%= header %></h3>
    </div>
  <% end %>
  <%= yield %>
<% end %>
