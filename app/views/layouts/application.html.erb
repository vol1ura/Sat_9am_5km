<!DOCTYPE html>
<html>
  <head>
    <title>Sat 9am 5km</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render 'navbars/top' %>
    <div class="container">
      <%= yield %>
    </div>
    <%= render 'navbars/bottom' %>
  </body>
</html>
